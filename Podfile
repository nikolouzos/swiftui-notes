# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'
inhibit_all_warnings!

target 'swiftui-notes' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Firebase
	pod 'Firebase/Core'
	pod 'Firebase/Firestore'
  pod 'Firebase/Auth'

	# Logging & Debugging
	pod 'SwiftyBeaver'
	pod 'Fabric'
	pod 'Crashlytics'

	# Views

end
